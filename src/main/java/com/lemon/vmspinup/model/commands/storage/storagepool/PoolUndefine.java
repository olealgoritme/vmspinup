package com.lemon.vmspinup.model.commands.storage.storagepool;

import com.lemon.vmspinup.model.storage.VMStoragePool;

public interface PoolUndefine {
    void poolUndefine(VMStoragePool vmStoragePool);
}
